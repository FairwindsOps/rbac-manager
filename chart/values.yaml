# Default values for rbac-manager.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: quay.io/reactiveops/rbac-manager
  tag: latest
  pullPolicy: Always

resources:
  requests:
    cpu: 100m
    memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
